<h1>Kysymys 1/8</h1>

<h3>Koodi A</h3>

<pre class="sh_java">
Scanner lukija = new Scanner(System.in);

System.out.print("Kuinka vanha olet: ");
int ika = Integer.parseInt(lukija.nextLine());

if( ika > 0 ) {
    System.out.println("Tervetuloa!");
} else {
    System.out.println("Huijaat!");
}
</pre>    

<h3>Koodi B</h3> 
<pre  class="sh_java">
Scanner lukija = new Scanner(System.in);

System.out.print("Kuinka vanha olet: ");
int ika = Integer.parseInt(lukija.nextLine());

if( ika > 0 ) {
System.out.println("Tervetuloa!");
} else {
System.out.println("Huijaat!");
}
</pre>



<%= form_for @code_question, :url => wizard_path, :method => :put do |f| %>
  <p>Mielestäni...</p>
  
  <%= f.radio_button :no_indent, "A" %> <%= f.label :no_indent ,"Koodi A on parempi" %>  <br> 
  <%= f.radio_button :no_indent, "B" %> <%= f.label :no_indent ,"Koodi B on parempi" %>  
  <br>
  <%= f.radio_button :no_indent, "EOS" %> <%= f.label :no_indent , "En osaa sanoa" %><br>
  <br>
  <br>
  <%= f.label "Vapaamuotoinen perustelu:" %>
  <br>
  <%= f.text_area :no_indent_comment, :size => '15x4' %>
  <br>

  <%= f.submit "Seuraava", :class=>"btn btn-primary"%>

 <% end %>